#!/bin/sh

last_month=$(date --date="last month" +%Y-%m)
sent=.sent-messages
sent_archive=.sent-messages-$last_month

cd $HOME/Mail || exit

if [ -d $sent ] && ! [ -d $sent_archive ]; then
	echo "moving $sent to $sent_archive"
	mv $sent $sent_archive
	notmuch new
fi

